{% extends 'base.html' %}
{% load staticfiles %}

{% block title %}DBCON!{% endblock %}

{% block primary %}


<h1><a href="/">DataBase Connection Test</a>;</h2>


{% if latest_event_list %}

    <ul>
    {% for event in latest_event_list %}
		<h2>{{ event.date }}</h2>
		<li><a href="/dbcon/{{ event.id }}/tweets">{{ event.keyterms }}</a></li>
		<!--ul>
        {%for tweet in event.tweets %}
			<li>{{ tweet.text }}</li>
		{% endfor %}
		</ul-->
    {% endfor %}
    </ul>

    {% if is_paginated %}
        <div class="pagination">
            <span class="page-links">
                {% if page_obj.has_previous %}
                    <a href="/dbcon?page={{ page_obj.previous_page_number }}">Previous</a>
                {% endif %}
                <span class="page-current">
                    Page {{ page_obj.number }} of {{ page_obj.paginator.num_pages }}.
                </span>
                {% if page_obj.has_next %}
                    <a href="/dbcon?page={{ page_obj.next_page_number }}">Next</a>
                {% endif %}
            </span>
        </div>
    {% endif %}

{% else %}
    <p>No Tweets Found</p>
{% endif %}



    		     
{% endblock %}

